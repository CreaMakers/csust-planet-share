---
import "../styles/global.css";

interface Props {
  title?: string;
  description?: string;
}

const { title = "长理星球", description = "长理星球官方页面" } = Astro.props;
---

<html lang="zh-CN" class="h-full">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="description" content={description} />
    <meta name="color-scheme" content="light" />
    <meta name="theme-color" content="#f8fafc" />
    <title>{title}</title>
    <slot name="head" />
  </head>
  <body class="min-h-screen bg-zinc-50 text-zinc-900 antialiased">
    <div class="flex min-h-screen flex-col">
      <header class="fixed inset-x-0 top-0 z-50 px-4 pt-4 sm:px-6 sm:pt-6">
        <div class="mx-auto flex h-16 w-full max-w-5xl items-center rounded-2xl border border-zinc-200/80 bg-white/75 px-4 shadow-sm shadow-zinc-200/40 backdrop-blur-md">
          <a href="/" class="inline-flex items-center gap-3">
            <img src="/logo_transparent.png" alt="长理星球 Logo" class="h-10 w-10 object-contain" />
            <span class="text-lg font-semibold tracking-tight sm:text-xl"> 长理星球 </span>
          </a>
        </div>
      </header>

      <main class="flex min-h-0 flex-1 flex-col pt-20 sm:pt-24">
        <slot />
      </main>

      <footer class="border-t border-zinc-200/80 bg-white/80 py-5 text-center text-sm text-zinc-500 backdrop-blur-md">&copy; 2024-2026 CreaMakers</footer>
    </div>
  </body>
</html>
